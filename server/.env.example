DATABASE_URL=postgres://localhost:5432/triathlonapp
JWT_SECRET=change-me-to-a-long-random-string
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development

# From https://www.strava.com/settings/api — Authorization Callback Domain
# should be your API host (e.g. localhost for dev, without protocol/port).
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI=http://localhost:4000/api/strava/callback
